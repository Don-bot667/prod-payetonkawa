🎫 FIX : Exclure la route images de l'authentification
Modifier api-produits/app/routes.py
Remplace la route get_image par celle-ci (sans dépendance) :

# GET /products/uploads/{filename} : Servir les images (SANS authentification)
@router.get("/uploads/{filename}", dependencies=[])
async def get_image(filename: str):
    filepath = os.path.join(UPLOAD_DIR, filename)
    if not os.path.exists(filepath):
        raise HTTPException(status_code=404, detail="Image non trouvee")
    return FileResponse(filepath)
Le dependencies=[] écrase la dépendance du router et permet l'accès sans API Key.