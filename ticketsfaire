🎫 TICKET : Corriger l'affichage des images
Problème 1 : Route protégée par API Key
Fichier : api-produits/app/routes.py

Ligne actuelle (à la fin du fichier) :

@router.get("/uploads/{filename}")
async def get_image(filename: str):
Remplacer par :

@router.get("/uploads/{filename}", dependencies=[])
async def get_image(filename: str):
Problème 2 : URL des images dans le frontend
L'API retourne image_url: "/uploads/produit_16.jpg", mais le frontend doit construire l'URL complète.

Vérifie dans le frontend comment l'image est affichée.

Si tu as quelque chose comme :

<img src={produit.image_url} />
Change en :

<img src={`https://api.payetonkawa.ouzfa.com/products${produit.image_url}`} />
Ou ajoute une fonction helper dans api.ts :

export function getImageUrl(imageUrl: string | null | undefined): string {
    if (!imageUrl) return '/placeholder.jpg';
    return `${API_PRODUITS}/products${imageUrl}`;
}
✅ Checklist
Modifier api-produits/app/routes.py → ajouter dependencies=[]
Vérifier/modifier l'affichage des images dans les frontends
Push sur GitHub
Redéployer sur le serveur